export enum PaymentStatus {
  PENDING = "pending",
  VERIFIED = "verified",
  REJECTED = "rejected",
}

export enum FineStatus {
    UNPAID = "unpaid",
    PARTIAL = "partial",
    PAID = "paid",
    PENDING = "pending",
    WAIVED = "waived",
    }
